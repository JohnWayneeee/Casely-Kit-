
# /casely.generate

## Назначение
Генерация тест-кейсов по требованиям в стиле пользователя.

## Синтаксис
```
/casely.generate [путь_к_требованиям]
```
- Путь относительно `feature-requirements/`. Примеры: `login.md`, `ui/mock.png`.

## Поддерживаемые форматы требований (MVP)
- Текст: `md`, `txt`
- Изображения: `png`, `jpg` (OCR; точность зависит от качества)
- `docx/pdf` — не поддержаны в MVP (предложить конвертацию)

## Алгоритм
1. Проверить наличие `config/user-rules.mdc`:
   - Если есть — загрузить и использовать только его.
    - Если нет — предупредить и использовать fallback `config/default-rules.md`.
2. Загрузить файл требований из `feature-requirements/`. Если нет файла — вывести доступные файлы/подпапки.
3. Проанализировать требования:
   - Текст: функциональность, роли, сценарии, ограничения, данные/валидации.
   - Изображения: ключевые UI элементы, состояния, навигацию.
4. Спланировать покрытие: позитивные, негативные, граничные кейсы (если применимо).
5. Сгенерировать тест-кейсы строго в формате правил (таблицы/списки/заголовки, стиль шагов, терминология).
6. Сохранить в `generated-test-cases/{YYYY-MM-DD}_{название}_test-cases.md`.
7. Вывести статистику и путь к файлу.

## Интерактивные уточнения (при неполных данных)
Задай 2–3 вопроса, например:
```
🤔 Уточнения:
1. Какие роли пользователей критичны?
2. Есть ли ограничения по паролю/полям ввода?
3. Нужны ли негативные сценарии (ошибки/валидация)?
Ответьте или напишите "пропустить" для генерации по текущим данным.
```

## Шаблон успешного ответа
```
✅ Тест-кейсы созданы!
Всего: {N} (позитивные {P}, негативные {Nn}, граничные {G})
Файл: generated-test-cases/{YYYY-MM-DD}_{название}_test-cases.md
Хотите уточнить или добавить сценарии?
```

## Шаблон ошибок
- user-rules отсутствует:
```
❌ Персональные правила не найдены.
Использовать default-rules? (да/нет)
Или запустите: /casely.init Фамилия И.О.
```
- Файл требований не найден:
```
❌ Файл не найден: {путь}
Доступно в feature-requirements/:
{список}
```
- Формат не поддержан:
```
❌ Формат не поддержан (docx/pdf).
Сконвертируйте в md/txt или png/jpg.
```

